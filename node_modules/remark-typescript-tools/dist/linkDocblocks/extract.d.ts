import ts from 'typescript';
import * as tsdoc from '@microsoft/tsdoc';
export interface ExtractorSettings {
    tsconfig: string;
    basedir: string;
    rootFiles: string[];
}
export declare class Extractor {
    program: ts.Program;
    basedir: string;
    constructor({ tsconfig, rootFiles, basedir }: ExtractorSettings);
    findTokens(token: string, node: ts.Node): ts.Node[];
    getComment(token: string, fileName?: string, overload?: number): (tsdoc.DocComment & {
        parserContext: tsdoc.ParserContext;
        buffer: string;
        overloadSummary: tsdoc.DocBlock | undefined;
        overloadRemarks: tsdoc.DocBlock | undefined;
        examples: tsdoc.DocBlock[];
    }) | null;
    byTagName(name: string): (block: tsdoc.DocBlock) => boolean;
}
